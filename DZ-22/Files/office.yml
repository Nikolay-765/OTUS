---
- name: MAIN | Office PC configuration
  hosts: all
  gather_facts: yes
  become: yes
  
  tasks:
  - name: "Software installation"
    ansible.builtin.apt:
      name: "{{ packages }}"
      state: present
      update_cache: true
    vars:
      packages:
      - traceroute
    tags:
      - software_installation